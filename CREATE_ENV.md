# 📝 إنشاء ملف .env

## ⚠️ مهم: ملف .env محظور من التعديل التلقائي

يجب إنشاء ملف `.env` يدوياً في مجلد `hayat-ai/`

## 📋 الخطوات:

### 1. أنشئ ملف جديد باسم `.env` في مجلد `hayat-ai/`

### 2. انسخ المحتوى التالي:

```env
# ═══════════════════════════════════════
# OpenAI Configuration (مطلوب)
# ═══════════════════════════════════════
OPENAI_API_KEY=sk-proj-REPLACE-WITH-YOUR-ACTUAL-KEY-HERE
OPENAI_MODEL=gpt-4-turbo-preview
TEMPERATURE=0.7
MAX_TOKENS=2000

# ═══════════════════════════════════════
# Application URLs
# ═══════════════════════════════════════
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=Hayat Beauty Clinic

# ═══════════════════════════════════════
# Contact Info
# ═══════════════════════════════════════
NEXT_PUBLIC_WHATSAPP_NUMBER=00905362266054
NEXT_PUBLIC_PHONE_NUMBER=00905362266054
NEXT_PUBLIC_EMAIL=info@mediai.tr
```

### 3. استبدل `REPLACE-WITH-YOUR-ACTUAL-KEY-HERE` بمفتاح OpenAI الفعلي

## 🔑 الحصول على OpenAI API Key:

1. اذهب إلى: https://platform.openai.com/api-keys
2. سجل دخول أو أنشئ حساب
3. اضغط "Create new secret key"
4. انسخ المفتاح والصقه في `.env`

## ✅ التحقق:

بعد إنشاء الملف، تأكد من:
- [ ] الملف موجود في `hayat-ai/.env`
- [ ] `OPENAI_API_KEY` يحتوي على مفتاح صحيح
- [ ] لا توجد مسافات إضافية حول `=`

## 🚀 بعد ذلك:

```bash
cd hayat-ai
npm install
npm run dev
```






