# 📋 تعليمات إعداد ملف .env

## خطوات الإعداد:

### 1. إنشاء ملف .env
قم بنسخ ملف `.env.example` إلى `.env`:

```bash
# في Windows PowerShell:
Copy-Item .env.example .env

# أو في Git Bash / Linux / Mac:
cp .env.example .env
```

### 2. تحديث OPENAI_API_KEY
افتح ملف `.env` وحدّث `OPENAI_API_KEY` بمفتاحك الفعلي:

```env
OPENAI_API_KEY=sk-proj-your-actual-api-key-here
```

### 3. (اختياري) تخصيص الإعدادات
يمكنك تعديل:
- `OPENAI_MODEL`: النموذج المستخدم (gpt-4-turbo-preview, gpt-4, gpt-3.5-turbo)
- `TEMPERATURE`: درجة الإبداع (0.0-1.0)
- `MAX_TOKENS`: الحد الأقصى للرموز في الرد

### 4. تثبيت dotenv (إذا لم يكن مثبتاً)
```bash
npm install dotenv
```

### 5. تشغيل الاختبار
```bash
npx tsx src/scripts/test-ai.ts
```

## ⚠️ ملاحظات مهمة:

- **لا ترفع ملف `.env` إلى Git** - يحتوي على معلومات حساسة
- ملف `.env.example` آمن للرفع - لا يحتوي على مفاتيح حقيقية
- تأكد من أن `.env` في `.gitignore`













