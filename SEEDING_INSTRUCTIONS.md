# 🌱 تعليمات Seeding قاعدة البيانات

## الخطوات المطلوبة لتنفيذ النظام بالكامل

### 1️⃣ تطبيق Migrations

```bash
cd hayat-ai
npx prisma migrate dev --name add_doctor_patient_models
```

هذا الأمر سيقوم بـ:
- إنشاء migration جديد
- تطبيق التغييرات على قاعدة البيانات
- إضافة نماذج `Doctor` و `Patient` المحدثة

### 2️⃣ تشغيل Seeding Script

```bash
npx prisma db seed
```

أو مباشرة:

```bash
npx tsx prisma/seed.ts
```

**النتيجة المتوقعة:**
- ✅ 15 طبيب مع بيانات تركية واقعية
- ✅ 50 مريض مع بيانات تركية واقعية
- ✅ عيادة افتراضية (Hayat AI Clinic)

### 3️⃣ التحقق من البيانات

```bash
# افتح Prisma Studio للتحقق من البيانات
npx prisma studio
```

أو استخدم API:

```bash
# الحصول على جميع الأطباء
curl http://localhost:3000/api/doctors

# الحصول على جميع المرضى
curl http://localhost:3000/api/patients
```

### 4️⃣ ربط الواجهات بالبيانات الحقيقية

تم إنشاء API routes التالية:
- ✅ `/api/doctors` - CRUD للأطباء
- ✅ `/api/patients` - CRUD للمرضى
- ✅ `/api/portal/doctors` - محدث لاستخدام Doctor model

### 5️⃣ الخطوات التالية (اختيارية)

1. **ربط Portal Dashboard:**
   - تحديث `app/(portal)/portal/page.tsx` لاستخدام `/api/doctors`
   - تحديث صفحة الأطباء لاستخدام البيانات الحقيقية

2. **ربط صفحة المواعيد:**
   - إنشاء API route للمواعيد
   - ربط صفحة المواعيد بالبيانات الحقيقية

3. **إضافة Authentication:**
   - ربط تسجيل الدخول بنظام المصادقة
   - ربط البيانات بالمستخدمين الحقيقيين

## 📋 ملاحظات مهمة

- تأكد من وجود `DATABASE_URL` في ملف `.env`
- تأكد من أن قاعدة البيانات متصلة وتعمل
- بعد seeding، يمكنك استخدام البيانات في جميع أجزاء التطبيق

## 🔄 إعادة Seeding

إذا أردت إعادة ملء قاعدة البيانات:

```bash
# حذف جميع البيانات (احذر!)
npx prisma migrate reset

# ثم إعادة seeding
npx prisma db seed
```



