# 🚀 دليل الاختبار السريع

## الملفات المتوفرة

تم إنشاء ملفين للاختبار السريع:

1. **`quick-test.ps1`** - للأنظمة Windows (PowerShell)
2. **`quick-test.sh`** - للأنظمة Linux/Mac (Bash)

## كيفية الاستخدام

### على Windows (PowerShell)

```powershell
# الانتقال إلى مجلد المشروع
cd hayat-ai

# تشغيل الاختبار
.\quick-test.ps1
```

### على Linux/Mac (Bash)

```bash
# الانتقال إلى مجلد المشروع
cd hayat-ai

# جعل الملف قابل للتنفيذ
chmod +x quick-test.sh

# تشغيل الاختبار
./quick-test.sh
```

## ما يفحصه السكريبت

### 1️⃣ فحص الملفات الأساسية
- ✅ `.env` - ملف المتغيرات البيئية
- ✅ `package.json` - ملف التبعيات
- ✅ `src/app` - مجلد التطبيق
- ✅ `src/components` - مجلد المكونات
- ✅ `src/data` - مجلد البيانات
- ✅ `src/lib` - مجلد المكتبات
- ✅ `src/store` - مجلد الحالة

### 2️⃣ فحص Dependencies
- ✅ التحقق من وجود `node_modules`
- ✅ تثبيت التبعيات إذا كانت مفقودة

### 3️⃣ فحص TypeScript
- ✅ التحقق من عدم وجود أخطاء TypeScript
- ✅ فحص جميع الملفات `.ts` و `.tsx`

### 4️⃣ فحص البيانات (اختياري)
- ✅ تشغيل `src/scripts/check-data.ts` إذا كان موجوداً

### 5️⃣ بناء المشروع
- ✅ تشغيل `npm run build`
- ✅ التحقق من نجاح البناء

## النتيجة

بعد اكتمال الاختبار، ستحصل على:

- ✅ تقرير شامل بحالة المشروع
- ✅ قائمة بالمشاكل المكتشفة (إن وجدت)
- ✅ تعليمات للتشغيل

## بعد الاختبار

إذا نجح الاختبار:

```bash
# تشغيل الخادم المحلي
npm run dev

# ثم افتح المتصفح على:
http://localhost:3000
```

## ملاحظات

- ⚠️ إذا كان `node_modules` مفقوداً، سيتم تثبيته تلقائياً
- ⚠️ إذا فشل البناء، راجع الأخطاء في Terminal
- ⚠️ تأكد من وجود ملف `.env` مع جميع المتغيرات المطلوبة

## استكشاف الأخطاء

إذا واجهت مشاكل:

1. **مشاكل في Dependencies:**
   ```bash
   rm -rf node_modules package-lock.json
   npm install --legacy-peer-deps
   ```

2. **مشاكل في TypeScript:**
   ```bash
   npx tsc --noEmit
   ```

3. **مشاكل في البناء:**
   ```bash
   rm -rf .next
   npm run build
   ```

---

✅ **تم إنشاء الملفات بنجاح!**





